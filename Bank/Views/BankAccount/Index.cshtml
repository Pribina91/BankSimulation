@model IEnumerable<BankSimulation.Models.BankAccount>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.idAccountNumber)
        </th>

        <th>
            @Html.DisplayNameFor(model => model.currency.name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.balance)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.idAccountNumber)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.currency.name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.balance)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idAccountNumber }) |
            @Html.ActionLink("Details", "Details", new { id=item.idAccountNumber }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idAccountNumber })
        </td>
    </tr>
}

</table>
