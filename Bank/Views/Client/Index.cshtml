@model IEnumerable<BankSimulation.Models.Client>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.accounts.idAccountNumber)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.accounts.idAccountNumber)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idClient }) |
            @Html.ActionLink("Details", "Details", new { id=item.idClient }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idClient })
        </td>
    </tr>
}

</table>
